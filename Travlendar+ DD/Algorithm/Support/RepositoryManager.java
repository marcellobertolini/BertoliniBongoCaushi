package support;

import checkers.subCheckers.Message;

/**
 * One of the software's manager classes that will manage the logic concerning the operations on the Data Base
 */
public class RepositoryManager {
    public String addEvent(ActionParameters parameters) {
        //DB operations
        return "Event added correctly!";
    }
}
